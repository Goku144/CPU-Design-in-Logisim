## THE HACK ASSEMBLER
First you need to be in this directory "CPU-Design-in-Logisim\Software\Compiler" (The CPU-Design-in-Logisim is the workspace folder)

and then run

```bash
.\bin\HackComp <input.asm> <output.hack>
```

you can chose the path of input <input.asm?> and the path of the output <output.hack>?